- Fix Problems with App not waiting for the api callback to be finished
- Get PlayerStatus to work
- Test if localStorage has a name in it. If not ask for name
- have a "change name button so People can easily change the name saved in the local storage"